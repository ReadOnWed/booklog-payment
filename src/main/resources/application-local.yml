# MySQL Configuration
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # DB Source URL
    url: jdbc:mysql://localhost:3306/Booklog?serverTimezone=Asia/Seoul
    # DB username
    username: readonwed
    # DB password
    password: dev3306

  jpa:
    # true 설정시 JPA 쿼리문 확인 가능
    show-sql: true
    # JPA의 구현체인 Hibernate가 동작하면서 발생한 SQL의 가독성을 높여줌
    properties:
      hibernate:
        format_sql: true

  # redis
  data:
    redis:
      port: 6379
      host: localhost
      password: test